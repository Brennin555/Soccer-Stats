import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html', // Verifique se o arquivo existe
  styleUrls: ['./app.component.scss'] // Verifique se o arquivo existe
})
export class AppComponent {
  title = 'meu-app'; // ou o nome do seu aplicativo
}
